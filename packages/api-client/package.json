{
  "name": "@sixtyfour-demos/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Thin TypeScript wrapper around api.sixtyfour.ai. Used by the site, the demos, and the provisioning script.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "eslint": "^8.57.0",
    "typescript": "^5.5.3"
  }
}
